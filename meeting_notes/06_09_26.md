


# discussion of error conditions
errno has a set number of values that come from the stlib
if each of the error conditions fall within these constants, then use them, else create our perfect
errors communicate the idea if he says liam turn on the light, do you have to do anything? No, the lights on. You could have said "no, the lights arlready on". That is wasting time. It's better to just say "success!".
I would like to remove this entry. If the entry isn't there, then just return success. 

# include a fully fledged example that showcases the utilization of the API as well as the error codes, and how a user would handle them.

Note: if an error were to occur, then it would be handled at that immediate block of code. 

the oneshot flag and the APS CSR are functionally equivalent


who generates the indexes? That's the API?


have man hw breakpoints should we have realistically? For now, we can assume we have 64 hw breakpoints? 4 is a reasonable start







is there a way to create any program that results in a control flow graph such that all basic blocks have an out degree of 2. 
(How does this relate to hw breakpoints?)


recusrion is forbidden becuase it has an out degree of 2, (and function pointers are forbidden)
Maybe don't use jump? Case where is does not jump but only branches


