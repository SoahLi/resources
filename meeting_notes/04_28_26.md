have some C support, I would like to use this to expand the impact of this idea. Once we get anticipation done, there is another thing to move onto. Suport into modifying gcc or lvm to make use of the mod instruction. 

do we have an opcode for return from handler? Ans: we haven't decided on that yet. I'm not sure if MRET and SRET are analogous, but I should be able to find an opcode for that. 

APR, this instruction, we need to have an encoding for, in the assembler. After this, we might just hvae you dive into gem5 and indetify where the CSR's are. 

10:00 AM 


Our goal is to get the mechanism done on gem5, and then start doing some testing, and comparison. Once we get the mechanism done, we'll need to revist (from the CPE side), for publication, C on the CS side - we want a submission by october - we need to have resluts much bfeore then. 

On the compiler side:

you can have an interrupt handler void int_handler(){}

If you look into the implimentatoin of sigint(), you register ineruppt hanlder with signint, you need to know if your returning from interrupt or the stack. In assembler you 
If you in an interrupt, you used iret insteda of returning to a strack pointer. sigint has to do this, and we'll then know how to impliment the anticipation return. 

The C ___ has 
There is GlibC, and it allows you to register a signal handler. 

The state when you power on is M for firmware.

Talk to Peterson


My task: Investigate the sigint function to see how return handlers are implimented in interrupts versus ____ (I think this name is function or stack based return)

TLA - Three letter acronym 


we are 4 or 5 steps away from REgintova. (he said once the operation mechanism is functional)

Send Tessler organization names

Hamming codes

survivorship bias - fallacy associated with selection.
