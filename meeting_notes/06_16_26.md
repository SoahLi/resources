



the AP handler expects the state of the program to be the same as the place that it was called from, i.e the anticipation point. This means that all GPRs should be untouched



We need to avoid modifying them on the way to the handler. 

Note: add to the man page, "return at your own risk"

Note: create a symbol (function, macro, etc) that impliments the assembly version of APRET


If ONESHOT is set, set the APS to one, else set it to 0 (continually re enter the loop)
we don't necessarily need a trampoline. We just need something that can 

