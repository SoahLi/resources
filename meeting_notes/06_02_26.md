

we don't have an output parameter of the previous hanlder because we have multiple handlers per address (make this ambiguous). The comment is "if the user wants to have multiple handlers they will have to walk to vector to determine if it's safe". I.e it;s your responsibility to remember what handlers you've registered

register - registering of the same handler twice means that you are modifying the existing register that was arlready assigned

if the user sets active to true, they exepect the AP to be enabled, i.e the.

if you register two handlers for the same address, the order of execution is indefined

when you close a scope or when you return from a handler, you need to call APRET


