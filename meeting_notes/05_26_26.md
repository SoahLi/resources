


# hello


chuthulu, hp lovecraft. 

took inspiration from Po and others. The way that he talks about horror is mostly unseen. Does a lot of passing through dimensions by accident. The feeling of dread is more important, and the entrapment and inescapability. The mountains of madness


gem5 splits the opcode. They don't exactly follow the riscv sepcification. 


iret - MRET, and SRET but in x86. We are using custom APRET

how to halt the processor? custom APRET. 

How to interupt these accesses. We don't hace an understanding of how execution is modeled. There is documentation on adding support for a new chip. That should give us a general idea of how you impliment a new processor. RISCV simple, and a three stage pipeline of RISCV


stub - function sum(int x, int ) {}
just get it in there and have it do nothing. Just to prove that it actually hits the stub. 






the general structure of the api is register something and unregister something. 
you can register multiple handlers for a single signal
register (target, function) - the target represents the address of the instruction being antixcipated. 
unregister
the other things that we need to be able to do are:


a void has special meaning in C. Is a value that fits in a register. It is the natural width of a register


the scheduler can use instruxtions, so target in the view of the scheduler targets specific instruxtions. To the perspective of the user, you can 







